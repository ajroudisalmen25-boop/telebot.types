import telebot, os from telebot.types import InlineKeyboardMarkup, InlineKeyboardButton  TOKEN = os.getenv("TOKEN") bot = telebot.TeleBot(TOKEN)  @bot.message_handler(commands=['start']) def start(m):     kb = InlineKeyboardMarkup()     kb.add(InlineKeyboardButton("📸 انستغرام", url="https://instagram.com/z_d_x_l_o_r_d"))     bot.send_message(m.chat.id, "مرحبا بيك!", reply_markup=kb)  bot.infinity_polling()
